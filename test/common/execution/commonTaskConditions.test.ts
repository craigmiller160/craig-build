
describe('commonTaskConditions', () => {
    describe('executeIfNotPreRelease', () => {
        it('is pre-release', () => {
            throw new Error();
        });

        it('is not pre-release', () => {
            throw new Error();
        });
    });

    describe('executeIfApplication', () => {
        it('is application', () => {
            throw new Error();
        });

        it('is not application', () => {
            throw new Error();
        });
    });
});
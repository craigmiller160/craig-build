import fs from 'fs';

describe('createTask', () => {
    it('creates executable task', () => {
        throw new Error();
    });

    it('creates task that skips execution', () => {
        throw new Error();
    });
});
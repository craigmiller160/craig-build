import fs from 'fs';

describe('runCommand', () => {
    it('test successful command', () => {
        throw new Error();
    });

    it('test failed command', () => {
        throw new Error();
    });

    it('test no status command', () => {
        throw new Error();
    });
});

describe('isNpmVersionInRange', () => {
	it('tests', () => {
		throw new Error();
	});

	describe('absolute versions', () => {
		it('greater than', () => {
			throw new Error();
		});

		it('less than', () => {
			throw new Error();
		});

		it('equal to', () => {
			throw new Error();
		});
	});

	describe('^/~ versions', () => {
		it('greater than', () => {
			throw new Error();
		});

		it('less than', () => {
			throw new Error();
		});

		it('equal to', () => {
			throw new Error();
		});
	});

	describe('range versions', () => {
		it('greater than', () => {
			throw new Error();
		});

		it('less than', () => {
			throw new Error();
		});

		it('equal to', () => {
			throw new Error();
		});
	});
});

export {};

describe('validateGitTag', () => {
	it('skips pre-release project', () => {
		throw new Error();
	});

	it('confirms there is no existing tag for release version', () => {
		throw new Error();
	});

	it('finds an existing tag for release version', () => {
		throw new Error();
	});
});

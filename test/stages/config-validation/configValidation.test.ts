import fs from 'fs';

describe('configValidation stage', () => {
    it('completes successfully', () => {
        throw new Error();
    });

    it('completes with error', () => {
        throw new Error();
    });
});

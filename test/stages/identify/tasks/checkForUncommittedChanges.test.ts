import * as TE from 'fp-ts/TaskEither';

describe('checkForUncommittedChanges', () => {
    it('there are uncommitted changes', () => {
        throw new Error();
    });

    it('there are not uncommitted changes', () => {
        throw new Error();
    });
});
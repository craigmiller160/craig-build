import fs from 'fs';

describe('getNexusProjectInfo task', () => {
    it('get Maven Nexus Project Info', () => {
        throw new Error();
    });

    it('get NPM Nexus Project Info', () => {
        throw new Error();
    });
});
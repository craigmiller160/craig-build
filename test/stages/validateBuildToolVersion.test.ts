export {};

describe('validateBuildToolVersion', () => {
	it('tool version is highest release version', () => {
		throw new Error();
	});

	it('tool version is not highest release version', () => {
		throw new Error();
	});

	it('user allows tool with pre-release version to run', () => {
		throw new Error();
	});

	it('user does not allow tool with pre-release version to run', () => {
		throw new Error();
	});
});

export {};

describe('validateDependencyVersions', () => {
	it('skips validation for pre-release maven project', () => {
		throw new Error();
	});

	it('skips validation for pre-release npm project', () => {
		throw new Error();
	});

	it('validates release dependencies for maven project', () => {
		throw new Error();
	});

	it('validates release dependencies for npm project', () => {
		throw new Error();
	});
});

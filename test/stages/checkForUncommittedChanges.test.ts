export {};

describe('checkForUncommittedChanges', () => {
	it('uncommitted changes found', () => {
		throw new Error();
	});

	it('uncommitted changes not found', () => {
		throw new Error();
	});
});

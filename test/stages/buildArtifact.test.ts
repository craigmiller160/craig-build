export {};

describe('buildArtifact', () => {
	it('skips docker projects', () => {
		throw new Error();
	});

	it('builds maven artifact', () => {
		throw new Error();
	});

	it('builds npm artifact', () => {
		throw new Error();
	});
});

import fs from 'fs';

describe('bumpNpmBeta task', () => {
    it('bumps beta version', () => {
        throw new Error();
    });

    it('bumps beta version when there is no beta number', () => {
        throw new Error();
    });
});
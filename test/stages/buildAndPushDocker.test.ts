export {};

describe('buildAndPushDocker', () => {
	it('no docker username environment variable', () => {
		throw new Error();
	});

	it('no docker password environment variable', () => {
		throw new Error();
	});

	it('skips for npm library', () => {
		throw new Error();
	});

	it('skips for maven library', () => {
		throw new Error();
	});

	it('builds and pushes docker image for maven application', () => {
		throw new Error();
	});

	it('builds and pushes docker image for npm application', () => {
		throw new Error();
	});

	it('builds and pushes docker image for docker application', () => {
		throw new Error();
	});

	it('builds and pushes docker image for docker image', () => {
		throw new Error();
	});
});

export {};

describe('downloadArtifactForDeployment', () => {
	it('skips for Docker project', () => {
		throw new Error();
	});

	it('downloads maven release artifact', () => {
		throw new Error();
	});

	it('downloads maven pre-release artifact', () => {
		throw new Error();
	});

	it('downloads npm release artifact', () => {
		throw new Error();
	});

	it('downloads npm pre-release artifact', () => {
		throw new Error();
	});
});

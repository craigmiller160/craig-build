export {};

describe('getProjectType', () => {
	it('is NpmLibrary', () => {
		throw new Error();
	});

	it('is MavenLibrary', () => {
		throw new Error();
	});

	it('is NpmApplication', () => {
		throw new Error();
	});

	it('is MavenApplication', () => {
		throw new Error();
	});

	it('is DockerApplication', () => {
		throw new Error();
	});

	it('is DockerImage', () => {
		throw new Error();
	});
});

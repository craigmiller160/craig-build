import fs from 'fs';

describe('kubeDeploy task', () => {
    it('deploys with configmap', () => {
        throw new Error();
    });

    it('deploys without configmap', () => {
        throw new Error();
    });
});

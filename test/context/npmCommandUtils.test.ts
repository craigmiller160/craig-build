describe('npmCommandUtils', () => {
	describe('getNpmCommand', () => {
		it('is npm', () => {
			throw new Error();
		});

		it('is yarn', () => {
			throw new Error();
		});

		it('is pnpm', () => {
			throw new Error();
		});

		it('is unknown', () => {
			throw new Error();
		});
	});
});
